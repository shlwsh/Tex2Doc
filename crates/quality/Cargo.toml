[package]
name        = "doc-quality"
version     = "0.1.0"
edition.workspace     = true
rust-version.workspace = true
license.workspace     = true
publish      = false
description = "V2 PDF 流水线 · 路径 C：结构 / 文本 / 视觉三层质量对比"

[dependencies]

[dev-dependencies]
