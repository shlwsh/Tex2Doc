# DOCX 结构语法抽取：sh

- 文件：`examples/paper3/output/to-docx/v6-论文稿件-jos-sh-20260617-225340.docx`
- 段落数：300
- 表格数：12
- Drawing 数：10
- 媒体文件数：10

## 段落样式分布

| Style | Count |
|---|---:|
| `JOSBody` | 114 |
| `JOSReference` | 78 |
| `JOSHeading2` | 38 |
| `JOSCaption` | 21 |
| `JOSImage` | 10 |
| `JOSHeading1` | 8 |
| `JOSCitation` | 6 |
| `(none)` | 4 |
| `JOSHeading3` | 4 |
| `JOSCode` | 4 |
| `JOSReferenceHeading` | 3 |
| `JOSInstituteZh` | 2 |
| `JOSKeywords` | 2 |
| `JOSTitleZh` | 1 |
| `JOSAuthorZh` | 1 |
| `JOSAbstractZh` | 1 |
| `JOSBodyNoIndent` | 1 |
| `JOSEnglishTitle` | 1 |
| `JOSAbstractEn` | 1 |

## 块序列摘要

| # | Type | Style/Shape | Text Preview |
|---:|---|---|---|
| 0 | paragraph | `JOSTitleZh` runs=1 drawing=0 | 基于动态关注清单的微服务日志定向采集方法 |
| 1 | paragraph | `JOSAuthorZh` runs=1 drawing=0 | 石 洪 雷, 赵 涓 涓 |
| 2 | paragraph | `JOSInstituteZh` runs=1 drawing=0 | （太原理工大学，山西太原 030024） |
| 3 | paragraph | `JOSInstituteZh` runs=1 drawing=0 | 通讯作者: 石洪雷, E-mail: shihonglei0042@link.tyut.edu.cn |
| 4 | paragraph | `(none)` runs=0 drawing=0 |  |
| 5 | paragraph | `JOSAbstractZh` runs=1 drawing=0 | 摘 要: 微服务架构下，日志来源高度分散、格式各异，总量随并发请求线性增长，节点资源、网络带宽和集中存储的压力随之显著增加。传统全量采集模式难以兼顾高价值日志保留与系统开销控制。本文提出一种基于动态关注清单的微服务日志定向采集方法，核心思路 |
| 6 | paragraph | `JOSKeywords` runs=1 drawing=0 | 关键词: 微服务; 分布式日志采集; 动态关注度评分; 定向过滤; 参数敏感性; Grafana Loki |
| 7 | paragraph | `JOSBodyNoIndent` runs=1 drawing=0 | 中图法分类号: TP311 |
| 8 | paragraph | `(none)` runs=0 drawing=0 |  |
| 9 | paragraph | `JOSCitation` runs=1 drawing=0 | 中文引用格式: 石洪雷, 赵涓涓. 基于动态关注清单的微服务日志定向采集方法. 软件学报. |
| 10 | paragraph | `JOSCitation` runs=1 drawing=0 | http://www.jos.org.cn/1000-9825/0000.htm |
| 11 | paragraph | `JOSCitation` runs=1 drawing=0 | 英文引用格式: Shi HL, Zhao JJ. Dynamic attention list-based directed log collection method for |
| 12 | paragraph | `JOSCitation` runs=1 drawing=0 | microservices. Ruan Jian Xue Bao/Journal of Software (in Chinese). http://www.jos.org.cn/1000-9825/0000.htm |
| 13 | paragraph | `(none)` runs=0 drawing=0 |  |
| 14 | paragraph | `JOSEnglishTitle` runs=1 drawing=0 | Dynamic Attention List-Based Directed Log Collection Method for Microservices |
| 15 | paragraph | `JOSCitation` runs=1 drawing=0 | SHI Hong-Lei, ZHAO Juan-Juan |
| 16 | paragraph | `JOSCitation` runs=1 drawing=0 | (College of Computer Science and Technology, Taiyuan University of Technology, Taiyuan 030024, China) |
| 17 | paragraph | `(none)` runs=0 drawing=0 |  |
| 18 | paragraph | `JOSAbstractEn` runs=1 drawing=0 | Abstract: In microservice architectures, log sources are highly distributed with heterogeneous formats, and their aggreg |
| 19 | paragraph | `JOSKeywords` runs=1 drawing=0 | Key words: microservice; distributed directed log collection; dynamic attention scoring; directed filtering; parameter s |
| 20 | paragraph | `JOSHeading1` runs=1 drawing=0 | 1 引言 |
| 21 | paragraph | `JOSBody` runs=5 drawing=0 | 微服务架构把一个大系统拆成多个独立服务，带来了更好的可扩展性和开发灵活性。容器编排、Sidecar 代理和服务治理等技术的成熟，使云原生应用的大规模部署成为现实[1-6]。拆分也带来一个棘手问题：日志高度分散。每个服务实例各自输出日志，格式 |
| 22 | paragraph | `JOSBody` runs=5 drawing=0 | 目前的研究主要集中在日志存储压缩、采样过滤、故障诊断和 eBPF 无侵入采集等方向，但很少有人从网关流量感知的角度出发，动态驱动各节点的应用日志定向采集。包航宇等[11]总结了智能运维的实践现状与标准化框架，贾统等[12]系统梳理了基于日志 |
| 23 | paragraph | `JOSBody` runs=1 drawing=0 | 本文关注的核心问题是：在不侵入业务代码、不改变微服务调用链的前提下，能否利用网关层的流量异常信号，从采集前端大幅降低日志入库量，同时保留异常和慢请求的高价值上下文？围绕这个核心问题，本文拆解出四个科学子问题：（1）如何从采集前端实现大幅减量 |
| 24 | paragraph | `JOSBody` runs=1 drawing=0 | 本文提出以下科学假设：（H1）网关流量驱动的关注清单能使定向采集模式下的 Loki 入库量相比同架构全量采集显著下降，同时在规则级保留异常与慢请求的关键上下文。（H2）定向采集的 Sidecar 资源开销（CPU、内存）不高于同架构全量采集 |
| 25 | paragraph | `JOSBody` runs=1 drawing=0 | 基于上述问题与假设，本文提出分布式定向日志采集框架（Distributed Directed Log Collection Framework）。框架分为三层：网关预筛选、节点定向采集、中心二次过滤，三层之间通过三次策略转换保持语义一致。和 |
| 26 | paragraph | `JOSBody` runs=1 drawing=0 | （1）三层协同定向采集架构。网关节点和微服务节点均以 Sidecar 方式部署，中心负责策略生成与二次过滤，节点负责本地匹配、缓存与可靠上传，构建了“流量感知—定向采集—语义一致入库”的完整闭环。这个架构将日志减量逻辑从传统的后端治理前置到 |
| 27 | paragraph | `JOSBody` runs=1 drawing=0 | （2）动态关注度评分模型（DASM）。提出以频次、错误率、延迟严重度和热度趋势四个归一化因子加权求和的动态评分机制（式 (1)），结合指数时间衰减（式 (2)）和负载自适应权重调节，实时生成高价值 URL 模式清单并下发至各节点（算法 1， |
| 28 | paragraph | `JOSBody` runs=1 drawing=0 | （3）定向策略三次转换机制。通过统一的 URL 泛化函数和清单版本控制，确保同一关注清单在网关预筛选、节点定向采集和中心二次过滤三层之间的语义一致性，避免跨层策略漂移（图 2）。 |
| 29 | paragraph | `JOSBody` runs=1 drawing=0 | （4）资源受限环境下的可靠传输架构。在 Sidecar 中引入固定缓存块（环形队列+双约束）和压力感知指数退避机制（式 (3)），辅以 BoltDB 本地兜底，在资源压力下提供有界内存占用和有界重试能力。 |
| 30 | paragraph | `JOSBody` runs=1 drawing=0 | （5）真实部署验证。在 8 微服务 DSB-Lite 真实部署环境下，n=5 配对实验显示 DASM 减量 25.9% ± 0.9%（p<0.001），且不引入额外延迟开销。 |
| 31 | paragraph | `JOSBody` runs=1 drawing=0 | （6）与 OpenTelemetry 尾采样的工程对比。在相同负载下对比 OTel Collector v0.103 + Tail Sampling，验证了 DASM 在高价值端点保留率和决策延迟两个维度上的优势。 |
| 32 | paragraph | `JOSBody` runs=1 drawing=0 | 基于 Go、gRPC、Redis 和 Grafana Loki 构建了可复现原型和多进程模拟器。在八节点集群上完成 180 s、n=10 的重复对比实验（95% CI 自助法估计）、算法微基准测试与系统级组件消融，并补充了 DSB-Lite |
| 33 | paragraph | `JOSBody` runs=1 drawing=0 | 本文第 2 节综述相关工作；第 3 节介绍系统总体设计；第 4 节阐述关键算法；第 5 节描述实现细节；第 6 节给出实验与分析；第 7 节总结全文。 |
| 34 | paragraph | `JOSHeading1` runs=1 drawing=0 | 2 相关工作 |
| 35 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.1 集中式日志栈 |
| 36 | paragraph | `JOSBody` runs=5 drawing=0 | ELK/EFK 以 Elasticsearch 为核心，检索能力强，但索引成本高昂。多项综述指出，大规模系统的日志在采集、传输、存储、解析和查询各阶段都会带来显著开销[7-9,13]。为降低这些开销，已有研究提出日志压缩、模式挖掘、模板解析 |
| 37 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.2 采样与追踪关联 |
| 38 | paragraph | `JOSBody` runs=9 drawing=0 | OpenTelemetry 尾部采样依赖分布式追踪上下文，在请求完成后决定是否保留相关 span 或日志[18]。追踪、指标和日志三类遥测数据在微服务排障中往往需要联合分析[10,19-20]。国内研究围绕分布式追踪、调用链存储、服务依赖发 |
| 39 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.3 OpenTelemetry 采样生态 |
| 40 | paragraph | `JOSBody` runs=5 drawing=0 | OpenTelemetry Collector 的 Tail Sampling Processor 支持基于延迟、状态码、属性等条件的尾部采样，但其决策粒度为 trace span 而非应用日志[18]。Adaptive Sampling（ |
| 41 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.4 LLM 与深度学习日志分析 |
| 42 | paragraph | `JOSBody` runs=7 drawing=0 | 大语言模型和 Transformer 架构在日志分析领域进展较快。LogGPT[32] 利用 GPT 模型进行日志异常检测，无需人工标注即可实现高精度异常识别。LILAC[33] 提出利用 LLM 进行日志解析，通过上下文学习将非结构化日志 |
| 43 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.5 边缘采集与可靠传输 |
| 44 | paragraph | `JOSBody` runs=5 drawing=0 | 边缘计算和云原生场景对采集组件提出了严苛要求：在低带宽、低资源或中心暂时不可达时，组件需保持有界退化[10,35-36]。Service Mesh 研究也表明 Sidecar 自身会引入额外延迟和资源消耗[37-38]，采集逻辑必须严格控制 |
| 45 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.6 AIOps 实践与标准化 |
| 46 | paragraph | `JOSBody` runs=5 drawing=0 | 包航宇等[11]基于大规模企业调研，总结了智能运维的实践现状，并提出了 AIOps-OSA 能力建设框架。后续研究也表明，生产系统需要将运行时数据、自动化分析和工程治理打通成闭环[20,39-40]。本文聚焦其中数据采集能力的前端减量环节： |
| 47 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.7 日志解析与故障诊断 |
| 48 | paragraph | `JOSBody` runs=9 drawing=0 | 贾统等[12]聚焦日志收集之后的下游环节，系统梳理了日志解析、异常检测、故障定位和知识提取等技术。在日志解析方面，Logram、模板识别和大规模解析评测等工作推动了非结构化日志向结构化事件的转换[17,41-42]。在异常检测方面，CNN- |
| 49 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.8 微服务工程与云原生生态 |
| 50 | paragraph | `JOSBody` runs=5 drawing=0 | 微服务架构涉及服务拆分、持续工程治理与运行时配置管理等多个方面[3,51-53]。容器编排和微服务设计模式为 Sidecar 部署提供了基础设施支撑[1-2,4]。鉴于此，本文在实验中明确区分了三类证据：原型集群实测、基于真实分布的多进程模 |
| 51 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.9 新兴采集技术与最接近先例 |
| 52 | paragraph | `JOSBody` runs=3 drawing=0 | eBPF 技术使内核级日志与事件采集成为可能[38,54]。eBPF 侧重基础设施级事件，对应用级日志的定向采集支持有限，且缺乏网关流量驱动的动态策略源。OpenTelemetry 尾部采样以分布式追踪 span 为单位做取舍，与本文以UR |
| 53 | paragraph | `JOSBody` runs=1 drawing=0 | 与本文最接近的工业实践有三类。其一，Envoy 代理的 AccessLog 过滤器支持按路由或状态码配置日志输出策略，但仅作用于代理自身的 access log，无法驱动下游微服务的应用日志采集。其二，Kong API 网关的 File-l |
| 54 | paragraph | `JOSBody` runs=1 drawing=0 | 上述先例存在两个共同局限：日志过滤决策局限于单一节点（代理或网关），且多依赖静态或手工配置规则。本文的增量创新在于：以网关流量统计为输入，自动生成 Top-K URL 模式清单，经 gRPC 下发至多个微服务节点驱动应用日志定向采集，最后由 |
| 55 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.10 与代表性方案对比 |
| 56 | paragraph | `JOSBody` runs=1 drawing=0 | 表 1 从策略输入、减量位置、日志削减证据、动态策略来源和 Sidecar 开销等维度，将本文方案与代表性方案进行对比。 |
| 57 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 1 与代表性日志采集/可观测性方案对比 |
| 58 | table | 9x7 | 方案 \| 策略驱动源 \| 减量位置 \| 日志削减证据 \| 动态策略来源 \| Sidecar 开销 \| 本文关系 \| ELK/EFK 全量 |
| 59 | paragraph | `JOSHeading2` runs=1 drawing=0 | 2.11 本文定位 |
| 60 | paragraph | `JOSBody` runs=7 drawing=0 | 从上述分析可以看出，包航宇等的 AIOps 标准化研究着力解决平台能力建设问题[11]，贾统等的日志诊断综述着力解决后端分析方法体系问题[12]。二者都需要稳定、低成本且高价值的日志输入作为基础。本文定位为AIOps 采集前端的定向减量技术 |
| 61 | paragraph | `JOSBody` runs=2 drawing=0 | *八节点、180 s、n=3；全量基线发射频率（400 ms）高于定向模式（2 s），降幅为策略过滤与源配置联合效果；基于真实分布的多进程同频模拟表明，策略过滤独立降幅为 67.8%（§6.3）。 |
| 62 | paragraph | `JOSHeading3` runs=1 drawing=0 | 2.11.1 OpenTelemetry 采样生态 |
| 63 | paragraph | `JOSBody` runs=1 drawing=0 | OpenTelemetry 生态提供了多种采样策略：Tail Sampling Processor（基于 trace 错误率/延迟的后置概率采样）、Adaptive Sampling（Uber Jaeger 的自适应采样）、Collecto |
| 64 | paragraph | `JOSHeading1` runs=1 drawing=0 | 3 系统总体设计 |
| 65 | paragraph | `JOSHeading2` runs=1 drawing=0 | 3.1 架构概述 |
| 66 | paragraph | `JOSBody` runs=1 drawing=0 | 本文框架采用三层协同定向采集架构，由两个核心角色组成：日志节点采集器（Agent）和日志中心（Center）。Agent 有两种部署形态——网关节点实例和微服务节点实例。网关实例通过 OpenResty Lua 采集 HTTP 流量日志，微 |
| 67 | paragraph | `JOSBody` runs=1 drawing=0 | 从能力边界看，整个框架可分为三层：网关预筛选、节点定向采集、中心二次过滤，如表 2 所示。三层分别负责运行时流量感知、本地日志减量和中心入库控制，每层的输入输出都可以独立审计，也方便与 AIOps 平台的数据采集能力对接。 |
| 68 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 2 分布式定向日志采集框架的三层输入/输出 |
| 69 | table | 4x4 | 层级 \| 输入 \| 输出 \| 作用 \| 网关预筛选 \| HTTP access log、状态码、响应时延 \| 高价值流量日志与 URL 模式候选 \| 从入口流量识别错误、慢请求和热点异常模式 |
| 70 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 71 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 1 分布式定向日志采集系统总体架构 |
| 72 | paragraph | `JOSHeading2` runs=1 drawing=0 | 3.2 工作流程 |
| 73 | paragraph | `JOSBody` runs=1 drawing=0 | 系统的运行过程分为以下五个步骤： |
| 74 | paragraph | `JOSBody` runs=1 drawing=0 | 1. 网关在 log_by_lua 阶段采集 URL、状态码、响应时延等字段，写入共享内存缓冲区； |
| 75 | paragraph | `JOSBody` runs=1 drawing=0 | 2. 网关 Agent 定期从缓冲区拉取流量日志，通过 gRPC 上传到 Center，Center 将其缓存到 Redis； |
| 76 | paragraph | `JOSBody` runs=1 drawing=0 | 3. Center 对流量日志做高价值过滤和 URL 聚类，生成 Top-K 关注清单，再通过 gRPC 下发给各节点； |
| 77 | paragraph | `JOSBody` runs=1 drawing=0 | 4. 微服务 Agent 拿到清单后，匹配本地应用日志，命中的日志写入固定缓存块，异步压缩上传； |
| 78 | paragraph | `JOSBody` runs=1 drawing=0 | 5. Center 做二次过滤和去重，最后批量推送到 Loki，供 Grafana 查询使用。 |
| 79 | paragraph | `JOSHeading2` runs=1 drawing=0 | 3.3 部署模式 |
| 80 | paragraph | `JOSBody` runs=1 drawing=0 | Sidecar 模式：Agent 与业务容器共享网络命名空间（network_mode: service:*），适用于 Kubernetes 或 Docker Compose 环境。混合开发模式：基础设施跑在容器里，Center 和 Age |
| 81 | paragraph | `JOSHeading2` runs=1 drawing=0 | 3.4 复杂度分析 |
| 82 | paragraph | `JOSBody` runs=1 drawing=0 | 设集群节点数为 N，URL 模式数为 M（实测约 20），Top-K 大小为 K（默认 7），历史窗口数为 W（默认 3）。本方法的关键复杂度如下： |
| 83 | paragraph | `JOSBody` runs=1 drawing=0 | itemize 时间复杂度：清单生成 O(M log K) per node；Center 聚合 O(N · M · log K)；在 1000 节点规模下理论耗时 < 50 ms（实测 8 节点 2.3 ms）。 空间复杂度：O(N ·  |
| 84 | paragraph | `JOSHeading1` runs=1 drawing=0 | 4 关键算法 |
| 85 | paragraph | `JOSBody` runs=1 drawing=0 | 本节详述框架的三个核心机制：关注清单如何从网关流量中动态生成、定向策略如何在三层之间保持语义一致、节点侧如何在资源受限下保障可靠传输。 |
| 86 | paragraph | `JOSHeading2` runs=1 drawing=0 | 4.1 关注清单动态生成 |
| 87 | paragraph | `JOSBody` runs=15 drawing=0 | 输入：时间窗口内网关流量日志集合 L={l1,…,lN}；定向策略 S（响应时延阈值 T、错误码集合 E）；历史窗口统计 mathcalH。 输出：关注清单 A={(pi,wi)}，pi 为泛化 URL 模式，wi ∈ [0,1] 为动态关 |
| 88 | paragraph | `JOSHeading3` runs=1 drawing=0 | 4.1.1 动态关注度评分模型 |
| 89 | paragraph | `JOSBody` runs=1 drawing=0 | 简单的频次×严重度加权无法反映模式的时序变化趋势和负载特征差异。为此，本文提出动态关注度评分模型（Dynamic Attention Scoring Model, DASM），将四个归一化因子加权求和： |
| 90 | paragraph | `JOSCode` runs=1 drawing=0 | Score(u,t)=α Freq(u,t)+β Err(u,t)+gamma Delay(u,t)+delta Trend(u,t) (1) |
| 91 | paragraph | `JOSBody` runs=1 drawing=0 | 其中 α+β+gamma+delta=1（均非负），四个因子分别定义为： itemize Freq(u,t)=count(u)/max_v count(v)，归一化频次； Err(u,t)=err_count(u)/count(u)，错误率 |
| 92 | paragraph | `JOSBody` runs=1 drawing=0 | 为了让近期异常的权重更高，引入指数时间衰减： |
| 93 | paragraph | `JOSCode` runs=9 drawing=0 | wdecay(u, t) = sumi wi·exp\!(-lambda (t-ti)) (2) |
| 94 | paragraph | `JOSBody` runs=3 drawing=0 | 其中 lambda 为衰减系数（默认 0.1/s），ti 为第 i 个历史窗口的时间戳。 |
| 95 | paragraph | `JOSBody` runs=1 drawing=0 | 参数自适应。当全局错误率超过 10% 时，β 自动提升 50%；当全局平均延迟超过 2T 时，gamma 自动提升 50%；提升后重新归一化，确保权重系数之和为 1。默认均衡模式取 α=0.3，β=0.3，gamma=0.2，delta=0 |
| 96 | table | 17x5 | Algorithm 1: 基于 DASM 的关注清单动态生成 \| \| Input: 流量日志集合 L，策略 S=(T,E,K,TTL)，历史窗口 mathcalH \| \| Output: 关注清单 A \| 1 \| H ← ∅;  |
| 97 | paragraph | `JOSBody` runs=1 drawing=0 | 复杂度分析。线性扫描 N 条日志为 O(N)；评分计算为 O(M)（M 为泛化模式数，M≤ N）；Top-K 选取为 O(M log K)（最小堆）或 O(M log M)（排序）。总体复杂度 O(N+M log M)，上界可简写为 O(N |
| 98 | paragraph | `JOSBody` runs=17 drawing=0 | theorem[评分有界性] 对任意 URL 模式 u 和时间 t，Score(u,t)∈[0,1]。 theorem proof 四个因子 Freq、Err、Delay、Trend 的值域均为 [0,1]（Trend 经 sigmoid  |
| 99 | paragraph | `JOSBody` runs=1 drawing=0 | proposition[退化兼容] 当 delta=0（无趋势因子）且 lambda=0（无时间衰减）时，DASM 退化为静态加权 Top-K。 proposition |
| 100 | paragraph | `JOSBody` runs=1 drawing=0 | URL 泛化规则包括：纯数字路径段映射为 {id}，UUID 或长哈希段映射为 {uuid}，查询参数按键名归一化。如果遇到异常极稀疏的窗口，算法会退化为仅保留 ERROR 级别的兜底规则。后文微基准测试显示，处理 5000 条合成日志大约 |
| 101 | paragraph | `JOSHeading3` runs=1 drawing=0 | 4.1.2 DASM 权重选择与自适应机制 |
| 102 | paragraph | `JOSBody` runs=1 drawing=0 | 为避免权重选择依赖经验，本文在三种典型负载场景（正常 / 错误密集 / 延迟密集）下进行网格搜索（α+β+gamma+delta=1 约束，步长 0.1，合法组合 60 个），以综合 F 值（F=sqrttext减量率 × text异常保留 |
| 103 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 3 DASM 权重配置寻优结果（三种负载场景） |
| 104 | table | 6x7 | 场景 \| α \| β \| gamma \| delta \| 减量率 \| F 值 \| Lnormal（正常流量） |
| 105 | paragraph | `JOSBody` runs=1 drawing=0 | 实验表明，本文默认均衡权重（α=0.3, β=0.3, gamma=0.2, delta=0.2）在多数场景下接近最优（综合 F 值差距 <5%），验证了权重设置的合理性。同时，自适应机制可在负载偏移时（错误密集 to 提升 β；延迟密集  |
| 106 | paragraph | `JOSHeading2` runs=1 drawing=0 | 4.2 固定缓存块 |
| 107 | paragraph | `JOSBody` runs=1 drawing=0 | 每个节点预先分配一块固定大小的内存 B（默认 64–128 MB），内部组织为环形队列，同时约束条目数和字节数。当占用率超过阈值 θ（默认 80%）或定时器触发时，就异步做 gzip 压缩上传；队列满了则按 FIFO 策略淘汰最旧的条目，确 |
| 108 | paragraph | `JOSHeading2` runs=1 drawing=0 | 4.3 定向策略三次转换 |
| 109 | paragraph | `JOSBody` runs=1 drawing=0 | 为了让策略在不同层级之间保持语义一致，本文设计了三次转换机制（表 4、图 2）： |
| 110 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 4 定向策略三次转换阶段说明 |
| 111 | table | 4x4 | 阶段 \| 输入 \| 输出 \| 作用 \| 第一次 \| 定向策略 S \| 网关采集规则 \| Nginx 预筛选（阈值 T/5） |
| 112 | paragraph | `JOSBody` runs=1 drawing=0 | 三次转换能正确工作，靠的是两个前提：同一关注清单版本号 v 和同一 URL 泛化函数 Generalize(·)。第一次转换用一个较宽松的阈值筛选流量日志，目的是不在网关侧过早丢掉潜在异常。第二次转换把候选集合压缩为 Top-K URL 模 |
| 113 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 114 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 2 定向策略三次转换算法流程 |
| 115 | paragraph | `JOSHeading2` runs=1 drawing=0 | 4.4 压力感知指数退避 |
| 116 | paragraph | `JOSBody` runs=1 drawing=0 | 上传失败时，第 n 次重试前的等待时间定义为 |
| 117 | paragraph | `JOSCode` runs=13 drawing=0 | dn = min(dmax, d0ρn + rand(0, d0ρnξ)). (3) |
| 118 | paragraph | `JOSBody` runs=7 drawing=0 | 其中 n 是当前重试序号，d0=200 ms 是初始延迟，ρ=2.0 是指数增长因子，dmax=30 s 是单次等待的上界，ξ=0.3 是随机抖动系数，rand(·) 是均匀随机扰动。当前实现最多重试 6 次；节点资源压力大时 dn 会翻倍 |
| 119 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 120 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 3 指数退避重试延迟分布 |
| 121 | paragraph | `JOSBody` runs=9 drawing=0 | 由于 dn 被 dmax 截断，且最大重试次数固定为 6，单批日志的在线重试等待时间有明确的上界：正常压力下不超过 6dmax，高压翻倍时不超过 12dmax。超过重试上限后，这批日志会进入本地 BoltDB 兜底队列，后续由后台恢复任务按 |
| 122 | paragraph | `JOSHeading1` runs=1 drawing=0 | 5 系统实现 |
| 123 | paragraph | `JOSHeading2` runs=1 drawing=0 | 5.1 技术选型 |
| 124 | paragraph | `JOSBody` runs=1 drawing=0 | 原型系统的主要实现技术如表 5 所示，组件选择围绕轻量部署、可靠传输和可复现实验展开。 |
| 125 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 5 技术选型 |
| 126 | table | 7x2 | 模块 \| 技术 \| Agent / Center \| Go 1.22 \| 通信 \| gRPC + Protobuf \| 缓存 \| Redis |
| 127 | paragraph | `JOSHeading2` runs=1 drawing=0 | 5.2 模块划分 |
| 128 | paragraph | `JOSBody` runs=1 drawing=0 | 系统实现分为 Agent 和 Center 两大模块。Agent 负责节点侧的工作，包括日志采集（collector）、规则匹配（matcher）、固定缓存块管理（cache）、退避重试（retry）、gRPC 上传（uploader）和资 |
| 129 | paragraph | `JOSHeading2` runs=1 drawing=0 | 5.3 关键数据结构与匹配实现 |
| 130 | paragraph | `JOSBody` runs=1 drawing=0 | 关注清单在 Redis 中按版本存储。Center 生成新清单后，先写入版本化键，再原子更新当前版本指针。Agent 拉取时会携带本地版本号，只有版本变化或 TTL 快要过期时才更新规则，减少不必要的网络往返。 |
| 131 | paragraph | `JOSBody` runs=7 drawing=0 | 节点侧匹配器将 URL 模式编译为前缀规则和通配规则，并按服务名分桶。单条日志匹配复杂度近似为 O(Rs)，其中 Rs 是该服务当前的关注规则数。在 Top-K 限制下，Rs 通常远小于全局规则数。应用日志采用 Apache Combine |
| 132 | paragraph | `JOSHeading2` runs=1 drawing=0 | 5.4 部署配置 |
| 133 | paragraph | `JOSBody` runs=1 drawing=0 | Docker Compose 定义了完整的集群环境，包括 Loki、Grafana、Redis、Prometheus、Center、OpenResty 网关、httpbin 模拟微服务以及 9 个 Agent Sidecar（1 个网关 + |
| 134 | paragraph | `JOSBody` runs=1 drawing=0 | 对照实验的配置说明：定向模式下应用日志的发射间隔是 2 s；全量基线模式是 400 ms（吞吐量更高），用来模拟工业场景中“尽可能多采”的做法。两种模式共享同一套网关压测负载和硬件环境，确保对比公平。 |
| 135 | paragraph | `JOSHeading2` runs=1 drawing=0 | 5.5 生产部署与标准化接口 |
| 136 | paragraph | `JOSBody` runs=1 drawing=0 | 在 Kubernetes 环境中，网关 Agent 可以部署为 Sidecar 或 DaemonSet，微服务 Agent 优先以 Sidecar 方式与业务容器共享网络命名空间。如果集群的日志文件路径比较统一，也可以用 DaemonSet |
| 137 | paragraph | `JOSBody` runs=1 drawing=0 | 生产部署需要启用 gRPC TLS、Agent 身份认证和租户隔离。关注清单的键空间按租户、命名空间和服务名分层，防止跨租户的规则泄露。在标准化方面，关注清单可以公开为一份 JSON 规范，包含 version、ttl、service、pa |
| 138 | paragraph | `JOSHeading1` runs=1 drawing=0 | 6 实验与分析 |
| 139 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.1 环境与方法 |
| 140 | paragraph | `JOSBody` runs=1 drawing=0 | 实验在 WSL2/Docker 原型集群上进行。主对比实验使用 8 节点配置，规模复核实验扩展到 16 和 32 个 httpbin 微服务及对应的 Sidecar Agent。集群还包含 1 个 OpenResty 网关、1 个日志中心以 |
| 141 | paragraph | `JOSBody` runs=3 drawing=0 | 对比实验将定向采集和全量采集（COLLECTION_MODE=full）放在同一硬件环境下对照。负载为 180 s、并发 50 的混合 HTTP 请求（包含正常请求、500 错误和慢请求），每种模式独立重复 3 次。Loki 入库增量取自  |
| 142 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.2 研究问题与指标 |
| 143 | paragraph | `JOSBody` runs=1 drawing=0 | 为了让实验与架构贡献形成清晰的对应关系，本文围绕 5 个研究问题组织实验，如表 6 所示。RQ1 验证采集前端的减量能力（对应贡献 1、2），RQ2 验证 Sidecar 资源开销的可控性（对应贡献 1），RQ3 验证三层策略的端到端贯通（ |
| 144 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 6 研究问题、指标与证据映射 |
| 145 | table | 6x4 | 编号 \| 研究问题 \| 指标 \| 证据位置 \| RQ1 \| 定向采集能否显著降低 Loki 入库日志量？ \| Loki 入库条数、带宽估计、降低比例 \| 表 7、图 4 |
| 146 | paragraph | `JOSBody` runs=1 drawing=0 | 统计分析。为提升统计严谨性，本文将重复实验扩样至 n=10（95% CI 由自助法 10000 次估计），并在主表中报告均值±标准差与置信区间（表 7）。Wilcoxon 符号秩检验显示，Loki 入库量差异的 p<0.01（dapprox |
| 147 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 7 定向采集与全量采集统计分析（八节点，180 s，n=10 配对实验，95% CI 自助法估计） |
| 148 | table | 8x6 | 指标 \| 定向采集 \| 全量采集 \| 降幅 \| p 值 \| Cohen's d \| Loki 入库 (条/180s) \| 71.70±1.22 [70.92,72.34] |
| 149 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 150 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 4 定向采集与全量采集资源消耗对比（八节点，180 s，n=10 配对实测） |
| 151 | paragraph | `JOSBody` runs=1 drawing=0 | 效应量分析。对 Loki 入库量（71.70±1.22 vs.\ 4392.18±16.12），Cohen's dapprox378，效应量极大；n=10 配对 Wilcoxon 检验 p<0.01，统计功效完全可靠。对 Agent CPU |
| 152 | paragraph | `JOSBody` runs=1 drawing=0 | 发射频率敏感性分析。全量基线的发射频率（400 ms）高于定向模式（2 s），为了把策略过滤的贡献单独剥离出来，本文构建了一个多进程集群模拟器做同频率对照实验。该模拟器以 phase3 实测负载分布、URL 结构和节点数为输入，采用虚拟时间 |
| 153 | paragraph | `JOSBody` runs=1 drawing=0 | 图 5 是 RQ4 的策略仿真消融结果。为了补充系统级组件消融证据，本文还做了多进程模拟消融：关闭关注清单后 Loki 入库量激增 211.7%（退化为全量），这说明清单匹配是减量的绝对核心。关闭二次过滤和固定缓存块对入库总量没有显著影响（ |
| 154 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 155 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 5 消融实验结果（策略仿真，非集群实测） |
| 156 | paragraph | `JOSBody` runs=1 drawing=0 | 对于 RQ3，端到端链路已在集群中实测打通：Nginx 共享内存 → Agent gRPC → Redis → 关注清单生成 → Agent 规则拉取。对于 RQ4，关注清单生成在 5000 条日志上耗时约 15 ms（图 6）；指数退避模 |
| 157 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 158 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 6 关注清单生成算法微基准 |
| 159 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.3 可复现性与效度威胁 |
| 160 | paragraph | `JOSBody` runs=1 drawing=0 | 全部实验脚本、Docker Compose 配置与原始 JSON 结果均保存在项目仓库的 experiments/ 目录下，按 phase3（主对比）、phase4（规模复核）和 phase5（多进程模拟）分目录组织。表 7 和图 4 由  |
| 161 | paragraph | `JOSBody` runs=1 drawing=0 | 规模效度：八节点主对比和十六/三十二节点规模复核是 WSL/Docker 实测，六十四节点仍为基于实测曲线的外推。云环境下的长稳表现与带宽压力还需要进一步验证。 |
| 162 | paragraph | `JOSBody` runs=1 drawing=0 | 基线效度：本文已给出同架构全量实测基线、Promtail 静态过滤的同负载模拟复现，以及 OpenTelemetry 与 eBPF 的同负载估算（图 8）。但 Promtail、OpenTelemetry 和 eBPF 的完整端到端部署对照 |
| 163 | paragraph | `JOSBody` runs=1 drawing=0 | 负载效度：当前负载由 httpbin 合成，覆盖了正常、错误和慢请求三类场景，但和 DeathStarBench、Alibaba 生产迹等真实微服务流量相比仍有差距。 |
| 164 | paragraph | `JOSBody` runs=1 drawing=0 | 统计效度：三期对比为 180 s、n=3；四期规模矩阵为 60 s、n=3；五期多进程模拟同样重复 3 次，但本质上仍是基于实测分布的模拟证据。逐探针命中率受 Agent 批处理影响，不能单独代表稳态漏报率。图 5 是策略仿真消融，不能与表 |
| 165 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.4 规模扩展与质量指标 |
| 166 | paragraph | `JOSBody` runs=1 drawing=0 | 四期实验中，本文补充了 16/32 节点定向模式的规模复核（60 s、并发 50、重复 3 次）。为避免历史计数干扰，复核前先执行 Redis 清空和 Center 重启，让 /api/v1/stats 从 0 开始计数。图 7 显示：16 |
| 167 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 168 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 7 多规模扩展实验（绿色/实线：8/16/32 节点集群实测；灰色/虚线：64 节点线性外推，非实测） |
| 169 | paragraph | `JOSBody` runs=3 drawing=0 | 漏报定义与度量。本文区分两类指标。第一类是规则级漏报——关注清单是否覆盖了所有满足定向策略条件（响应时延 >T 或状态码 ∈ E）的 URL 模式。第二类是探针即时命中率——某一时刻向指定服务发送探针请求后，对应的应用日志是否在 Agent |
| 170 | paragraph | `JOSBody` runs=1 drawing=0 | 端到端延迟与可接受性。端到端延迟（HTTP → Center received）实测结果：P50 为 0.83 s，P95 为 12 s（n=30）。P95 偏高主要是因为 Agent 2 s 批处理窗口和合成慢请求（如 /delay/1  |
| 171 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.5 工业基线对照 |
| 172 | paragraph | `JOSBody` runs=5 drawing=0 | 图 8 在同负载口径下给出了本文定向采集与三类工业基线的趋势对照。为了补充实证对比，本文在多进程模拟器中复现了 Promtail 的静态标签过滤（保留 status≥400 错误或 /delay/ 慢请求路由）。模拟结果显示，同负载分布下  |
| 173 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 174 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 8 工业基线趋势对比（P3 定向采集为集群实测；Promtail 为同负载多进程规则复现；OTel/eBPF 为同负载估算，柱形样式区分证据等级） |
| 175 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.6 真实微服务负载验证 |
| 176 | paragraph | `JOSBody` runs=3 drawing=0 | 为了验证本文方法在更复杂的微服务拓扑下的适用性，本文基于 DeathStarBench Social Network 的 21 个 API 端点分布[55]进行了模拟验证。DSB Social Network 包含 12 个微服务，其流量分 |
| 177 | paragraph | `JOSBody` runs=1 drawing=0 | 在标准配置（K=20, T=500 ms, 200 RPS, 180 s, n=3）下，定向采集相对全量的日志降幅为 5.6%，高价值日志（错误请求和慢请求）召回率为 95.1%。表 8 对比了两种负载的验证结果。 |
| 178 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 8 不同负载下定向采集效果验证 |
| 179 | table | 3x6 | 负载类型 \| URL 端点数 \| 全量 (条) \| 定向 (条) \| 降幅 \| 高价值召回 \| httpbin (合成, 8 节点实测) \| 8 |
| 180 | paragraph | `JOSBody` runs=1 drawing=0 | 这一结果揭示了一个重要的设计权衡：当 URL 端点数量接近 K 时，关注清单几乎覆盖所有端点，降幅自然减小。即便在 DSB 的 21 端点场景下，定向采集仍然实现了 94.9% 的高价值日志召回率，且在 50–1000 RPS 范围内保持稳 |
| 181 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 182 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 9 不同负载下的减量效果对比：(a) httpbin 与 DSB 的降幅/召回对比；(b) DSB 负载下不同 RPS 的扩展性 |
| 183 | paragraph | `JOSHeading3` runs=1 drawing=0 | 6.6.1 DSB-Lite 真实部署端到端验证 |
| 184 | paragraph | `JOSBody` runs=1 drawing=0 | §6.3 在 DSB 流量分布模拟器上验证了 DASM 算法的减量效果，但模拟负载无法反映真实部署中采集、传输、编码、网络与进程调度的全链路协同情况；本节进一步在 DSB-Lite 真实部署环境下执行 n=5 配对实验，端到端验证系统在真实 |
| 185 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 9 DSB-Lite 真实部署减量效果（n=5，配对设计） |
| 186 | table | 4x5 | 指标 \| Full \| Directed \| 差异 \| p 值 \| Loki 入库量（条/60s） \| 5160±4031 \| 3798±2940 |
| 187 | paragraph | `JOSBody` runs=1 drawing=0 | 关键发现：（1）DASM 在真实多服务场景下减量 25.9% ± 0.9%（p<0.001），这一结果与 §6.3 模拟负载下的高减量率方向一致；（2）Agent CPU 略有降低（6.20% vs 7.12%），系统本身并未因减量决策带来 |
| 188 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.7 与 OpenTelemetry 尾采样的对比 |
| 189 | paragraph | `JOSBody` runs=1 drawing=0 | 为与最相近的工程方案对比，本文在相同负载下部署 OpenTelemetry Collector v0.103（启用 Tail Sampling Processor），配置 errors / slow / probabilistic 5% 三 |
| 190 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 10 DASM 与 OpenTelemetry Tail Sampling 的对比（n=5） |
| 191 | table | 7x4 | 维度 \| DASM 定向采集 \| OTel Tail Sampling \| 优势方 \| 减量率 \| 22.9%±1.8% \| 92.3%±0.9% \| OTel |
| 192 | paragraph | `JOSBody` runs=1 drawing=0 | 对比分析：OTel 减量率绝对值更高（92.3% vs 22.9%），但其本质是概率采样，会随机丢弃包括高价值端点在内的所有请求（高价值端点保留率仅 5%）。DASM 在异常保留率均为 100% 的前提下，通过内容驱动的 Top-K 决策保 |
| 193 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.8 扩展性分析与超大规模外推 |
| 194 | paragraph | `JOSBody` runs=1 drawing=0 | 本文基于 O(N · M · log K) 理论模型对 1–1000 节点规模进行外推（表 11）。实测 8 节点清单生成耗时 2.3 ms，1000 节点理论外推 9.2 ms；Center CPU 在 1000 节点下预计 12.5%（ |
| 195 | paragraph | `JOSCaption` runs=1 drawing=0 | 表 11 DASM 关注清单生成的扩展性分析（理论模型 + 100/500/1000 节点外推） |
| 196 | table | 8x5 | 节点数 N \| 总 RPS \| 清单生成 (ms) \| Center CPU (%) \| 减量率 \| 1 \| 100 \| 2.30 |
| 197 | paragraph | `JOSBody` runs=1 drawing=0 | 扩展策略：单 Center 实例可承载约 1000 节点（CPU 12.5%）；10000 节点规模需 10 个 Center 实例，通过 URL 前缀分片（sharding by URL prefix）实现水平扩展。 |
| 198 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.9 剩余局限与后续工作 |
| 199 | paragraph | `JOSBody` runs=1 drawing=0 | 经过四期实验，本文已补充了 8/16/32 节点规模矩阵、规则级漏报验证、端到端延迟和工业基线估算。五期多进程模拟进一步补齐了同频率发射对照、Promtail 静态过滤对照和系统级组件消融方面的缺失环节。六期参数敏感性实验评估了 Top-K |
| 200 | paragraph | `JOSBody` runs=1 drawing=0 | 近期/中期工作：（1）在 Kubernetes 环境开展 64 节点云原生实测，替代当前基于 32 节点复核结果的趋势外推；（2）部署 DeathStarBench 或 Alibaba 生产迹的端到端实测，替代当前的模拟验证。 |
| 201 | paragraph | `JOSBody` runs=1 drawing=0 | 远期工作：（3）与 OpenTelemetry 追踪上下文、eBPF 应用日志挂钩集成，探索采集前减量与后端智能诊断（如根因定位）的无缝闭环；（4）推进系统关键组件的生产级能力，包括 gRPC TLS 安全通信、多租户策略隔离、可配置的隐私 |
| 202 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.10 参数敏感性分析（RQ5） |
| 203 | paragraph | `JOSBody` runs=1 drawing=0 | 为了评估 DASM 模型中关键超参数对系统性能的影响，本文基于多进程模拟器对 Top-K、TTL 和延迟阈值 T 三个参数进行扫描实验（每组 n=3）。 |
| 204 | paragraph | `JOSBody` runs=1 drawing=0 | Top-K 敏感性。在包含 45 个 URL 模式的长尾分布合成负载下，Top-K 取值对覆盖率有显著影响：K=10 时规则级覆盖率为 88.5%，K=20 时提升至 93.5%，K≥30 后达到 100% 并保持稳定。相应地，Loki 入 |
| 205 | paragraph | `JOSBody` runs=1 drawing=0 | 延迟阈值 T 敏感性。T 直接影响高价值日志的筛选范围。T=200 ms 时，大量请求被标记为慢请求，入库量达 77 条；T=5000 ms 时，仅极慢请求被采集，入库量降至 28 条。默认 T=500 ms 在入库量（61 条）和覆盖率（ |
| 206 | paragraph | `JOSBody` runs=1 drawing=0 | TTL 敏感性。在稳态负载下，TTL 取值（10–300 s）对入库量和覆盖率的影响不显著，这是因为模拟器中的窗口刚新频率高于 TTL 过期速度。在生产环境的突发流量场景下，过短的 TTL 可能导致清单频繁过期和覆盖不稳定，建议取值不低于窗 |
| 207 | paragraph | `JOSImage` runs=1 drawing=1 |  |
| 208 | paragraph | `JOSCaption` runs=1 drawing=0 | 图 10 参数敏感性分析：(a) Top-K 取值对入库量和覆盖率的影响；(b) 延迟阈值 T 对入库量和高价值日志数的影响 |
| 209 | paragraph | `JOSHeading2` runs=1 drawing=0 | 6.11 成本影响分析 |
| 210 | paragraph | `JOSBody` runs=1 drawing=0 | 基于八节点实测数据，本文估算了不同规模下定向采集带来的存储和带宽成本节约。假设每条日志约 512 字节，生产负载为实验负载的 1.5 倍，以 AWS S3 公开定价为基准： |
| 211 | paragraph | `JOSCode` runs=1 drawing=0 | CostSaving(n)=StorageSaving(n)+BandwidthSaving(n)+IngestSaving(n) (4) |
| 212 | paragraph | `JOSBody` runs=1 drawing=0 | 外推结果显示，100 节点时全量模式月存储约 573 GB，定向模式仅约 9.4 GB，年化节约约 \3 700；1000 节点时年化节约约 \37 000。这一估算基于 httpbin 合成负载外推，实际生产环境的日志量可能更高，节约效果 |
| 213 | paragraph | `JOSHeading1` runs=1 drawing=0 | 7 结束语 |
| 214 | paragraph | `JOSBody` runs=1 drawing=0 | 本文针对微服务分布式日志采集中高开销与低价值的矛盾，提出了一种网关驱动的分布式定向日志采集框架。主要架构贡献包括四个方面： |
| 215 | paragraph | `JOSBody` runs=1 drawing=0 | （1）三层协同定向采集架构。构建了网关预筛选、节点定向采集与中心二次过滤的三层架构，以 Sidecar 模式部署，将日志减量逻辑从后端治理前置到采集前端，形成“流量感知—定向采集—语义一致入库”的完整闭环。 |
| 216 | paragraph | `JOSBody` runs=1 drawing=0 | （2）网关流量驱动的动态关注清单生成。以网关流量日志作为策略源，经过高价值过滤、URL 泛化和 Top-K 筛选（算法 1），动态识别异常与慢请求的 URL 模式，生成关注清单并经 Redis 下发到各节点。定向采集决策由实际流量驱动，无需 |
| 217 | paragraph | `JOSBody` runs=1 drawing=0 | （3）定向策略三次转换算法。通过统一的 URL 泛化函数和清单版本控制，把网关预筛选、节点定向采集和中心二次过滤贯通起来，让策略在不同层级之间保持语义一致（图 2），避免跨层策略漂移。 |
| 218 | paragraph | `JOSBody` runs=1 drawing=0 | （4）资源受限下的可靠传输机制。在 Sidecar 中引入固定缓存块和压力感知指数退避，辅以 BoltDB 本地兜底，确保内存有界传输，适应边缘资源受限的环境。 |
| 219 | paragraph | `JOSBody` runs=1 drawing=0 | 基于 Go、gRPC、Redis 和 Loki 的原型在 WSL/Docker 八节点集群上完成了端到端验证，并通过 16/32 节点规模复核与基于真实分布的多进程模拟器，补充了同频率对照、Promtail 静态过滤复现和组件消融。和同架构 |
| 220 | paragraph | `JOSBody` runs=1 drawing=0 | 清空统计后的规模复核显示：16 节点网关流量日志增量为 6703.33±91.78 条/轮，定向模式 Loki 入库为 48.0±5.2 条/轮；32 节点网关流量日志增量为 6692.67±121.88 条/轮，定向模式 Loki 入库为 |
| 221 | paragraph | `JOSBody` runs=1 drawing=0 | 从 AIOps 生态的角度看，本文的工作补充了标准化框架中数据采集前端的能力。从日志故障诊断链路来看，本文降低了后端解析、异常检测和根因定位的输入规模。 |
| 222 | paragraph | `JOSBody` runs=1 drawing=0 | 未来工作按优先级排列：近期将在 Kubernetes 环境开展 64 节点云原生实测，并补充 Top-K 参数敏感性分析；中期将引入 DeathStarBench、Alibaba 生产迹等真实微服务负载，验证长尾分布下的覆盖率，同时构建成本 |
| 223 | paragraph | `JOSHeading1` runs=1 drawing=0 | 8 DSB-Lite 部署细节 |
| 224 | paragraph | `JOSHeading2` runs=1 drawing=0 | 8.1 服务清单 |
| 225 | paragraph | `JOSBody` runs=1 drawing=0 | DSB-Lite 保留 DeathStarBench Social Network 原 12 个微服务中的 7 个核心服务：user-service、post-service、social-graph-service、media-servi |
| 226 | paragraph | `JOSHeading2` runs=1 drawing=0 | 8.2 数据层适配 |
| 227 | paragraph | `JOSBody` runs=1 drawing=0 | 原版 DSB Social Network 使用 MongoDB（持久化）+ Memcached（缓存）。DSB-Lite 将两者统一替换为 Redis 单实例：MongoDB 的 Collection 操作（find/insert）映射为 |
| 228 | paragraph | `JOSHeading2` runs=1 drawing=0 | 8.3 部署脚本 |
| 229 | paragraph | `JOSBody` runs=1 drawing=0 | 本机原生部署，详见 deploy/deathstarbench/host_native/： itemize start_services.sh：启动 Redis + 8 个 Flask 微服务 stop_services.sh：停止所有服务 |
| 230 | paragraph | `JOSBody` runs=1 drawing=0 | 本文撰写与实验脚本生成过程中使用了大语言模型辅助，作者对全部内容与数据负责。 |
| 231 | paragraph | `JOSReferenceHeading` runs=1 drawing=0 | References |
| 232 | paragraph | `JOSReference` runs=1 drawing=0 | [1] Brendan Burns, David Oppenheimer, and Eric Brewer. Design patterns for container-based distributed systems. In Proce |
| 233 | paragraph | `JOSReference` runs=1 drawing=0 | [2] Brendan Burns, Brian Grant, David Oppenheimer, Eric Brewer, and John Wilkes. Borg, omega, and kubernetes. Communicat |
| 234 | paragraph | `JOSReference` runs=1 drawing=0 | [3] Pooyan Jamshidi, Claus Pahl, Nabor C. Mendonca, James W. Lewis, and Stefan Tilkov. Microservices: The journey so far |
| 235 | paragraph | `JOSReference` runs=1 drawing=0 | [4] Guilherme Vale, Filipe Figueiredo Correia, Eduardo Martins Guerra, Thatiane de Oliveira Rosa, Jonas Fritzsch, and Ju |
| 236 | paragraph | `JOSReference` runs=1 drawing=0 | [5] Zhiyong Feng, Yanwei Xu, Xiao Xue, and Shizhan Chen. Current status and prospects of microservice technology develop |
| 237 | paragraph | `JOSReference` runs=1 drawing=0 | [6] Huayao Wu and Wenjun Deng. Research progress on microservice-oriented software development methods. Journal of Compu |
| 238 | paragraph | `JOSReference` runs=1 drawing=0 | [7] Xiangke Liao, Shanshan Li, Wei Dong, Zhouyang Jia, Xiaodong Liu, and Shulin Zhou. A survey on logs in large-scale so |
| 239 | paragraph | `JOSReference` runs=1 drawing=0 | [8] Mingshu Li, Yan Zhang, Tao Wang, and Jun Liu. A survey on log management technologies for large-scale distributed sy |
| 240 | paragraph | `JOSReference` runs=1 drawing=0 | [9] Shilin He, Pinjia He, Zhuangbin Chen, Tianyi Yang, Yuxin Su, and Michael R. Lyu. A survey on automated log analysis  |
| 241 | paragraph | `JOSReference` runs=1 drawing=0 | [10] Muhammad Usman, Simone Ferlin, Anna Brunstrom, and Javid Taheri. A survey on observability of distributed edge and  |
| 242 | paragraph | `JOSReference` runs=1 drawing=0 | [11] Hangyu Bao, Kanglin Yin, Li Cao, Shining Li, et al. Practice of aiops: Status quo and standardization. Journal of S |
| 243 | paragraph | `JOSReference` runs=1 drawing=0 | [12] Tong Jia, Ying Li, and Zhonghai Wu. A survey on fault diagnosis of distributed software systems based on log data.  |
| 244 | paragraph | `JOSReference` runs=1 drawing=0 | [13] Yanning Chen, Guangyan Zhang, and Kang Chen. A universal anomaly detection mechanism for multi-syntax logs in cloud |
| 245 | paragraph | `JOSReference` runs=1 drawing=0 | [14] Jinyang Liu, Jieming Zhu, Shilin He, Pinjia He, Zibin Zheng, and Michael R. Lyu. Logzip: Extracting hidden structur |
| 246 | paragraph | `JOSReference` runs=1 drawing=0 | [15] Jieming Zhu, Shilin He, Pinjia He, Jinyang Liu, and Michael R. Lyu. Loghub: A large collection of system log datase |
| 247 | paragraph | `JOSReference` runs=1 drawing=0 | [16] Junyu Wei, Guangyan Zhang, and Junchao Chen. Data schema-aware low-cost cloud log storage system. Journal of Comput |
| 248 | paragraph | `JOSReference` runs=1 drawing=0 | [17] Zhihan Jiang, Jinyang Liu, Junjie Huang, Yichen Li, and Michael R. Lyu. Lilac: Log parsing using llms with adaptive |
| 249 | paragraph | `JOSReference` runs=1 drawing=0 | [18] Zhuangbin Chen, Zhihan Jiang, Yuxin Su, Michael R. Lyu, and Zibin Zheng. Tracemesh: Scalable and streaming sampling |
| 250 | paragraph | `JOSReference` runs=1 drawing=0 | [19] Abdullah Al Maruf, Alexander Bakhtin, Tomas Cerny, and Davide Taibi. Using microservice telemetry data for system d |
| 251 | paragraph | `JOSReference` runs=1 drawing=0 | [20] Sergio Moreschini, Davide Taibi, Valentina Lenarduzzi, and Claus Pahl. Ai techniques in the microservices life-cycl |
| 252 | paragraph | `JOSReference` runs=1 drawing=0 | [21] Yong Yang, Ying Li, and Zhonghai Wu. A survey on distributed tracing technologies. Journal of Software, 31(7):2019– |
| 253 | paragraph | `JOSReference` runs=1 drawing=0 | [22] Zicheng Huang, Pengfei Chen, Guangba Yu, and Hongyang Chen. Transparent request tracing and sampling method for jav |
| 254 | paragraph | `JOSReference` runs=1 drawing=0 | [23] Yong You, Hao Wang, Tian Ren, Shenghui Gu, and Jialin Sun. A storage design of link-tracing log data for monitoring |
| 255 | paragraph | `JOSReference` runs=1 drawing=0 | [24] Qingyang Yu, Xiaoying Bai, Mingjie Li, Qiyuan Li, Tao Liu, Zeyin Liu, and Dan Pei. Performance modeling and anomaly |
| 256 | paragraph | `JOSReference` runs=1 drawing=0 | [25] Qixun Zhang, Yifan Wu, Yong Yang, Tong Jia, Ying Li, and Zhonghai Wu. A survey on service dependency discovery tech |
| 257 | paragraph | `JOSReference` runs=1 drawing=0 | [26] Lu Wang, Yuxuan Jiang, Qingshan Li, Qien Huo, Zhan Wang, Shenglong Xie, and Jie Dai. A survey on microservice fault |
| 258 | paragraph | `JOSReference` runs=1 drawing=0 | [27] Zhiyu Wang, Tao Wang, Wenbo Zhang, Ningjiang Chen, and Chun Zuo. A microservice fault diagnosis method based on exe |
| 259 | paragraph | `JOSReference` runs=1 drawing=0 | [28] Li Wu, Johan Tordsson, Erik Elmroth, and Odej Kao. Microrca: Root cause localization of performance issues in micro |
| 260 | paragraph | `JOSReference` runs=1 drawing=0 | [29] Shenglin Zhang, Pengxiang Jin, Zihan Lin, Yongqian Sun, et al. Robust failure diagnosis of microservice system thro |
| 261 | paragraph | `JOSReference` runs=1 drawing=0 | [30] Tingting Wang and Guilin Qi. A comprehensive survey on root cause analysis in (micro) services: Methodologies, chal |
| 262 | paragraph | `JOSReference` runs=1 drawing=0 | [31] Cindy Sridharan. Distributed Systems Observability: A Guide to Building Robust Systems. O'Reilly Media, 2018. |
| 263 | paragraph | `JOSReference` runs=1 drawing=0 | [32] Jiaxing Qi, Shaohan Luan, Song Huang, and Carol Fung. Loggpt: Exploring chatgpt for log-based anomaly detection. ar |
| 264 | paragraph | `JOSReference` runs=1 drawing=0 | [33] Zhihan Jiang, Jinyang Liu, Zhuangbin Chen, Yichen Li, Junjie Huang, Yintong Huo, Pinjia He, Jiazhen Gu, and Michael |
| 265 | paragraph | `JOSReference` runs=1 drawing=0 | [34] Hongcheng Guo, Jian Yang, Jiaheng Liu, Jiaqi Bai, Boyang Wang, et al. Logformer: A pre-train and tuning pipeline fo |
| 266 | paragraph | `JOSReference` runs=1 drawing=0 | [35] Benjamin Varghese, Nikolaos Antonopoulos, and Rajkumar Buyya. A survey on edge performance benchmarking. ACM Comput |
| 267 | paragraph | `JOSReference` runs=1 drawing=0 | [36] Min Liu and Aoying Zhou. Container-level elastic resource provisioning method for microservice architecture. Journa |
| 268 | paragraph | `JOSReference` runs=1 drawing=0 | [37] Yuxin Zhang, Bowen Li, Xin Peng, Qilin Xiang, and Xuanzhe Liu. Technical report: Performance comparison of service  |
| 269 | paragraph | `JOSReference` runs=1 drawing=0 | [38] David Soldani, Petrit Nahi, Hami Bour, Saber Jafarizadeh, Mohammed F. Soliman, et al. ebpf: A new approach to cloud |
| 270 | paragraph | `JOSReference` runs=1 drawing=0 | [39] Qian Cheng, Doyen Sahoo, Amrita Saha, Wenzhuo Yang, Chenghao Liu, and Steven C. H. Hoi. Ai for it operations (aiops |
| 271 | paragraph | `JOSReference` runs=1 drawing=0 | [40] Rodrigo Laigner, Yixin Zhou, Alan Salomao, Pengfei Zhou, Leonardo Carvalho, Alexander Romanovsky, and Claus Pahl. A |
| 272 | paragraph | `JOSReference` runs=1 drawing=0 | [41] Hetong Dai, Heng Li, Weiyi Shang, Tse-Hsun Chen, and Che-Shao Chen. Logram: Efficient log parsing using n-gram dict |
| 273 | paragraph | `JOSReference` runs=1 drawing=0 | [42] Zhihan Jiang, Jinyang Liu, Junjie Huang, Yichen Li, Yintong Huo, et al. A large-scale evaluation for log parsing te |
| 274 | paragraph | `JOSReference` runs=1 drawing=0 | [43] Yudong Mei, Xu Chen, Yuzhong Sun, et al. A software system anomaly detection method based on log information and CN |
| 275 | paragraph | `JOSReference` runs=1 drawing=0 | [44] Shan Ali, Chaima Boufaied, Domenico Bianculli, Paula Branco, and Lionel Briand. A comprehensive study of machine le |
| 276 | paragraph | `JOSReference` runs=1 drawing=0 | [45] Fatemeh Hadadi, Joshua H. Dawes, Donghwan Shin, Domenico Bianculli, and Lionel Briand. Systematic evaluation of dee |
| 277 | paragraph | `JOSReference` runs=1 drawing=0 | [46] Xiaohui Zhou, Yijie Wang, Hongzuo Xu, and Mingyu Liu. Unsupervised multivariate time series anomaly detection based |
| 278 | paragraph | `JOSReference` runs=1 drawing=0 | [47] Haotian Fang, Chunhua Li, Qing Wang, and Ke Zhou. A deep learning-based microservice performance anomaly detection  |
| 279 | paragraph | `JOSReference` runs=1 drawing=0 | [48] Lingzhe Zhang, Tong Jia, Kangjin Wang, Mengxi Jia, Yong Yang, and Ying Li. Reducing events to augment log-based ano |
| 280 | paragraph | `JOSReference` runs=1 drawing=0 | [49] Lingzhe Zhang, Tong Jia, Mengxi Jia, Ying Li, Yong Yang, and Zhonghai Wu. Multivariate log-based anomaly detection  |
| 281 | paragraph | `JOSReference` runs=1 drawing=0 | [50] Jacopo Soldani and Antonio Brogi. Graph-based root cause analysis for service-oriented and microservice architectur |
| 282 | paragraph | `JOSReference` runs=1 drawing=0 | [51] Dan Ding, Xin Peng, Xiaofeng Guo, Jian Zhang, and Yijian Wu. Scenario-driven and bottom-up microservice decompositi |
| 283 | paragraph | `JOSReference` runs=1 drawing=0 | [52] Tong Jia, Ying Li, Qixun Zhang, and Zhonghai Wu. Logging position decision method based on program hierarchical tre |
| 284 | paragraph | `JOSReference` runs=1 drawing=0 | [53] Vishwanath Seshagiri, Darby Huye, Lan Liu, Avani Wildani, and Raja R. Sambasivan. [sok] identifying mismatches betw |
| 285 | paragraph | `JOSReference` runs=1 drawing=0 | [54] Wanqi Yang, Pengfei Chen, Kai Liu, and Huxing Zhang. Nahida: In-band distributed tracing with ebpf. arXiv preprint  |
| 286 | paragraph | `JOSReference` runs=1 drawing=0 | [55] Yu Gan, Yanqi Zhang, Daliang Cheng, Ankitha Shetty, Priber Rathi, Nayan Katarki, Arber Bruno, Justin Hu, Brian Ritc |
| 287 | paragraph | `JOSReferenceHeading` runs=1 drawing=0 | 附中文参考文献: |
| 288 | paragraph | `JOSReference` runs=1 drawing=0 | [5] 冯志勇, 徐砚伟, 薛霄, 陈世展. 微服务技术发展的现状与展望. 计算机研究与发展, 2020, 57(5): 1103–1122. |
| 289 | paragraph | `JOSReference` runs=1 drawing=0 | [6] 吴化尧, 邓文俊. 面向微服务软件开发方法研究进展. 计算机研究与发展, 2020, 57(3): 525–541. |
| 290 | paragraph | `JOSReference` runs=1 drawing=0 | [7] 廖湘科, 李姗姗, 董威, 贾周阳, 刘晓东, 周书林. 大规模软件系统日志研究综述. 软件学报, 2016, 27(8): 1934–1947. http://www.jos.org.cn/1000-9825/4910.htm |
| 291 | paragraph | `JOSReference` runs=1 drawing=0 | [8] 李明树, 张艳, 王涛, 刘俊. 大规模分布式系统日志管理技术综述. 软件学报, 2019, 30(7): 1959–1979. |
| 292 | paragraph | `JOSReference` runs=1 drawing=0 | [11] 包航宇, 殷康 璘, 曹立, 李世宁, 等. 智能运维的实践: 现状与标准化. 软件学报, 2023, 34(9): 4069–4095. |
| 293 | paragraph | `JOSReference` runs=1 drawing=0 | [12] 贾统, 李影, 吴中海. 基于日志数据的分布式软件系统故障诊断综述. 软件学报, 2020, 31(7): 1997–2018. |
| 294 | paragraph | `JOSReference` runs=1 drawing=0 | [13] 陈彦宁, 张广艳, 陈康. 面向云数据中心多语法日志通用异常检测机制. 计算机研究与发展, 2020, 57(9): 1844–1856. |
| 295 | paragraph | `JOSReference` runs=1 drawing=0 | [16] 魏钧宇, 张广艳, 陈军超. 数据模式感知的低成本云日志存储系统. 计算机研究与发展, 2023, 60(11): 2442–2452. |
| 296 | paragraph | `JOSReference` runs=1 drawing=0 | [21] 杨勇, 李影, 吴中海. 分布式追踪技术综述. 软件学报, 2020, 31(7): 2019–2039. |
| 297 | paragraph | `JOSReference` runs=1 drawing=0 | [22] 黄梓程, 陈鹏飞, 余广坝, 陈泓仰. 面向Java微服务系统的透明请求追踪及采样方法. 软件学报, 2023, 34(7): 3167–3187. |
| 298 | paragraph | `JOSReference` runs=1 drawing=0 | [23] 尤勇, 汪浩, 任天, 顾胜晖, 孙佳林. 一种监控系统的链路跟踪型日志数据的存储设计. 软件学报, 2021, 32(5): 1302–1321. |
| 299 | paragraph | `JOSReference` runs=1 drawing=0 | [24] 于庆洋, 白晓颖, 李明杰, 李奇原, 刘涛, 刘泽胤, 裴丹. 基于调用链控制流分析的大型微服务系统性能建模与异常定位. 软件学报, 2022, 33(5): 1849–1864. |
| 300 | paragraph | `JOSReference` runs=1 drawing=0 | [25] 张齐勋, 吴一凡, 杨勇, 贾统, 李影, 吴中海. 微服务系统服务依赖发现技术综述. 软件学报, 2024, 35(1): 118–135. |
| 301 | paragraph | `JOSReference` runs=1 drawing=0 | [26] 王璐, 姜宇轩, 李青山, 等. 微服务故障检测研究综述. 计算机学报, 2023, 46(11): 2342–2373. |
| 302 | paragraph | `JOSReference` runs=1 drawing=0 | [27] 王智宇, 王涛, 张文博, 陈宁江, 左春. 一种基于执行轨迹监测的微服务故障诊断方法. 软件学报, 2017, 28(6): 1435–1454. |
| 303 | paragraph | `JOSReference` runs=1 drawing=0 | [32] 刘敏, 周傲英. 面向微服务架构的容器级弹性资源供给方法. 计算机研究与发展, 2017, 54(5): 957–967. |
| 304 | paragraph | `JOSReference` runs=1 drawing=0 | [39] 梅御东, 陈旭, 孙毓忠, 等. 一种基于日志信息和CNN-text的软件系统异常检测方法. 计算机学报, 2020, 43(2): 524–544. |
| 305 | paragraph | `JOSReference` runs=1 drawing=0 | [43] 周小晖, 王意洁, 徐鸿祚, 刘铭宇. 基于融合学习的无监督多维时间序列异常检测. 计算机研究与发展, 2023, 60(3): 496–508. |
| 306 | paragraph | `JOSReference` runs=1 drawing=0 | [44] 方浩天, 李春花, 王清, 周可. 一种基于深度学习的微服务性能异常检测方法. 计算机研究与发展, 2024, 61(3): 600–613. |
| 307 | paragraph | `JOSReference` runs=1 drawing=0 | [48] 丁丹, 彭鑫, 郭晓峰, 张健, 吴毅坚. 场景驱动且自底向上的单体系统微服务拆分方法. 软件学报, 2020, 31(11): 3461–3480. |
| 308 | paragraph | `JOSReference` runs=1 drawing=0 | [49] 贾统, 李影, 张齐勋, 吴中海. 基于程序层次树的日志打印位置决策方法. 软件学报, 2021, 32(9): 2713–2728. |
| 309 | paragraph | `JOSReferenceHeading` runs=1 drawing=0 | 作者简介 |
| 310 | paragraph | `JOSReference` runs=1 drawing=0 | 石洪雷，博士，CCF专业会员，主要研究领域为微服务架构开发、智能体系统构建、智能运维与智能机器人应用开发。 E-mail: shihonglei0042@link.tyut.edu.cn |
| 311 | paragraph | `JOSReference` runs=1 drawing=0 | 赵涓涓，博士，教授，博士生导师，CCF杰出会员，主要研究领域为智能信息处理、模式识别、情感计算、图像处理。 E-mail: zh_juanjuan@126.com |
